package com.tribalframework.utils.smoothing{	import flash.net.*;	import flash.events.*;	import flash.geom.Matrix;	import flash.display.Bitmap;	import flash.display.*;		public class TribalSmoothing extends MovieClip	{		/*		**  private var link = "http://153.ddbstaging.com/mario/acm/10.png"			private var loader; 			loader = new Loader();			loader.contentLoaderInfo.addEventListener(Event.COMPLETE, kk);			loader.load(new URLRequest( link ));			mc.addChild( loySmooth(loader.content, 320/480) )		**		*********************/		public function TribalSmoothing(){					}		public static function smooth(pp:Bitmap, myScale:Number):Bitmap{			//var pp = loader.content;			var scale:Number = 1;			var matrix:Matrix = new Matrix();			matrix.scale(scale, scale);					var smallBMD:BitmapData = new BitmapData(pp.width * scale, pp.height * scale, false, 0x000000);			var bigBMD:BitmapData = new BitmapData(pp.width, pp.height, false, 0x000000);			bigBMD.draw(pp, matrix, null, null, null, true);			matrix.scale(myScale, myScale);									///			smallBMD.draw(bigBMD, matrix, null, null, null, true);			///						var bitmap:Bitmap = new Bitmap(smallBMD, PixelSnapping.NEVER, true);						return bitmap;		}	}	}	